const initialState = {
  isAuthenticated: false,
}

export default function update(state: Object = initialState, action: Object): Object {
  switch (action.type) {
    default:
      return state
  }
}
